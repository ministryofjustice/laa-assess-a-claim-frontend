apiVersion: v1
kind: Service
metadata:
  name: { { include "laa-assess-a-claim-frontend.fullname" . } }
  labels: { { - include "laa-assess-a-claim-frontend.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "laa-assess-a-claim-frontend.selectorLabels" . | nindent 4 } }
